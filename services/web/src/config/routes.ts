export enum MenuItem {
  CHART = 'chart',
  DATAFRAME = 'dataframe',
  PORTFOLIO = 'portfolio',
  CONFIG = 'config',
}

export enum Paths {
  HOME = '/',
  DATAFRAME = '/dataframe',
  PORTFOLIO = '/portfolio',
  CONFIG = '/config',
}
